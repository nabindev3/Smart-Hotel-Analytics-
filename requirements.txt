# requirements.txt — full dev/training environment.
# Top-level dependencies only; pip resolves the transitive ones.
# Serving image installs requirements.prod.txt, frontend image
# requirements.frontend.txt. Optional extras stay out on purpose:
# kagglehub (load_real_data tier-3 source), transformers/torch/datasets
# (knowledge_distillation --finetune) — both degrade gracefully when absent.

# Serving (superset of requirements.prod.txt)
fastapi==0.136.1
uvicorn==0.46.0
pydantic==2.13.3
requests==2.33.1

# Data / ML
pandas==2.2.3
numpy==2.2.2
pyarrow==23.0.1
scikit-learn==1.7.2
scipy==1.15.1
xgboost
lightgbm
prophet==1.3.0
shap==0.49.1
joblib==1.4.2
pandera==0.31.1

# Training / experiment tracking
mlflow==3.11.1
matplotlib==3.10.0

# NLP
anthropic==0.97.0
textblob==0.20.0

# Frontend (dashboard)
streamlit==1.56.0
plotly==6.7.0
